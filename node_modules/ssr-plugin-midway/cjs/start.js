"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.start = void 0;
const child_process_1 = require("child_process");
const ssr_server_utils_1 = require("ssr-server-utils");
const { cli } = require('@midwayjs/cli/bin/cli');
const start = (argv) => {
    const config = (0, ssr_server_utils_1.loadConfig)();
    (0, child_process_1.exec)('npx cross-env ets', async (err, stdout) => {
        if (err) {
            console.log(err);
            return;
        }
        console.log(stdout);
        // 透传参数给 midway-bin
        argv._[0] = 'dev';
        argv.ts = true;
        argv.port = config.serverPort;
        await cli(argv);
    });
};
exports.start = start;
